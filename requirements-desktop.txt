# Desktop application requirements for PyInstaller builds
# Install with: pip install -r requirements-desktop.txt

# Base application requirements
-r requirements.txt

# Qt GUI Framework
PySide6>=6.6.0

# Desktop packaging
pyinstaller>=6.0.0

# Credential storage (Linux)
secretstorage>=3.3.0; sys_platform == 'linux'

# Cryptography for cookie extraction
cryptography>=41.0.0

# Additional build dependencies
setuptools>=68.0.0
wheel>=0.41.0
